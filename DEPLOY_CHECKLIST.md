# ✅ Чеклист деплоя Frontend на VPS

Пошаговый чеклист для развертывания Gate149 Frontend на steins.ru

---

## Перед началом

- [ ] У вас есть доступ к VPS по SSH
- [ ] У вас есть домен steins.ru
- [ ] У вас есть доступ к DNS настройкам домена
- [ ] У вас есть работающий Backend API
- [ ] У вас есть работающий Ory Kratos

---

## Этап 1: Подготовка проекта (локально)

- [ ] Проверено наличие всех Docker файлов:
  - [ ] `frontend/Dockerfile`
  - [ ] `frontend/.dockerignore`
  - [ ] `frontend/docker-compose.yml`
  
- [ ] Обновлен `frontend/next.config.mjs`:
  - [ ] Добавлено `output: 'standalone'`

- [ ] Создан файл `frontend/.env.production`:
  - [ ] Указан `NEXT_PUBLIC_APP_URL`
  - [ ] Указан `BACKEND_API_URL`
  - [ ] Указан `ORY_SDK_URL`
  - [ ] Все URL валидны и доступны

- [ ] Проверена локальная сборка (опционально):
  ```bash
  cd frontend
  docker compose build
  docker compose up
  ```

---

## Этап 2: Настройка VPS

### 2.1 Подключение и обновление системы

- [ ] Подключение к VPS:
  ```bash
  ssh user@steins.ru
  ```

- [ ] Обновление системы:
  ```bash
  sudo apt update && sudo apt upgrade -y
  ```

### 2.2 Установка Docker

- [ ] Установлен Docker:
  ```bash
  curl -fsSL https://get.docker.com -o get-docker.sh
  sudo sh get-docker.sh
  ```

- [ ] Docker добавлен в автозагрузку:
  ```bash
  sudo systemctl enable docker
  sudo systemctl start docker
  ```

- [ ] Пользователь добавлен в группу docker (опционально):
  ```bash
  sudo usermod -aG docker $USER
  ```
  Затем выйдите и войдите снова.

- [ ] Проверка Docker:
  ```bash
  docker --version
  docker compose version
  ```

### 2.3 Настройка Firewall

- [ ] Установлен UFW:
  ```bash
  sudo apt install ufw
  ```

- [ ] Настроены правила:
  ```bash
  sudo ufw allow 22/tcp   # SSH
  sudo ufw allow 80/tcp   # HTTP
  sudo ufw allow 443/tcp  # HTTPS (для будущего)
  ```

- [ ] Включен firewall:
  ```bash
  sudo ufw enable
  ```

- [ ] Проверен статус:
  ```bash
  sudo ufw status
  ```

---

## Этап 3: Настройка DNS

- [ ] Открыта панель управления DNS провайдера

- [ ] Создана A-запись для домена:
  - Имя: `@` или пусто
  - Тип: `A`
  - Значение: `[IP вашего VPS]`
  - TTL: `3600`

- [ ] Создана A-запись для www (опционально):
  - Имя: `www`
  - Тип: `A`
  - Значение: `[IP вашего VPS]`
  - TTL: `3600`

- [ ] Проверка DNS (может занять до 48 часов):
  ```bash
  nslookup steins.ru
  ping steins.ru
  ```

---

## Этап 4: Загрузка файлов на VPS

### Вариант A: Через SCP/Rsync

- [ ] Создана директория на VPS:
  ```bash
  ssh user@steins.ru "mkdir -p ~/gate149"
  ```

- [ ] Файлы скопированы на VPS (с локальной машины):
  ```powershell
  # PowerShell
  scp -r frontend user@steins.ru:~/gate149/
  ```
  
  Или с исключениями:
  ```bash
  rsync -avz --exclude 'node_modules' --exclude '.next' --exclude '.git' frontend/ user@steins.ru:~/gate149/frontend/
  ```

### Вариант B: Через Git

- [ ] Репозиторий клонирован на VPS:
  ```bash
  ssh user@steins.ru
  cd ~
  git clone https://your-repo-url.git gate149
  ```

### Проверка

- [ ] Проверено наличие файлов:
  ```bash
  ssh user@steins.ru "ls -la ~/gate149/frontend"
  ```

---

## Этап 5: Сборка и запуск Docker контейнера

- [ ] Подключение к VPS:
  ```bash
  ssh user@steins.ru
  ```

- [ ] Переход в директорию проекта:
  ```bash
  cd ~/gate149/frontend
  ```

- [ ] Проверка .env файла:
  ```bash
  cat .env.production
  ```
  
  Если файл отсутствует или неверный:
  ```bash
  nano .env.production
  # Вставьте правильные значения
  ```

- [ ] Проверка docker-compose.yml:
  ```bash
  docker compose config
  ```

- [ ] Сборка Docker образа:
  ```bash
  docker compose build
  ```
  
  Примерное время: 5-15 минут

- [ ] Запуск контейнера:
  ```bash
  docker compose up -d
  ```

- [ ] Проверка статуса:
  ```bash
  docker compose ps
  ```
  
  Статус должен быть `Up` или `running`

---

## Этап 6: Проверка работоспособности

### 6.1 Проверка логов

- [ ] Логи просмотрены на наличие ошибок:
  ```bash
  docker compose logs
  ```

- [ ] Нет критических ошибок (ECONNREFUSED, build errors, etc.)

- [ ] Если ошибки есть, см. раздел Troubleshooting в DEPLOYMENT.md

### 6.2 Проверка доступности

- [ ] Сайт доступен локально на VPS:
  ```bash
  curl http://localhost
  ```
  
  Должен вернуть HTML код страницы

- [ ] Сайт доступен по IP адресу:
  ```bash
  curl http://[IP-VPS]
  ```

- [ ] Сайт доступен по домену (с локальной машины):
  - Открыть в браузере: `http://steins.ru`

- [ ] Проверка заголовков:
  ```bash
  curl -I http://steins.ru
  ```
  
  Должен вернуть `200 OK`

### 6.3 Функциональная проверка

- [ ] Главная страница загружается
- [ ] Навигация работает
- [ ] Стили (Mantine) применены
- [ ] Нет ошибок в консоли браузера (F12)
- [ ] API запросы работают (проверить в Network tab)
- [ ] Аутентификация работает (если настроен Kratos)

---

## Этап 7: Настройка HTTPS (Рекомендуется)

### 7.1 Установка Nginx

- [ ] Nginx установлен:
  ```bash
  sudo apt install nginx -y
  ```

- [ ] Nginx запущен:
  ```bash
  sudo systemctl start nginx
  sudo systemctl enable nginx
  ```

### 7.2 Настройка Nginx конфига

- [ ] Создан конфиг для сайта:
  ```bash
  sudo nano /etc/nginx/sites-available/steins.ru
  ```

- [ ] Скопирован конфиг из `frontend/nginx.conf.example`

- [ ] Конфиг активирован:
  ```bash
  sudo ln -s /etc/nginx/sites-available/steins.ru /etc/nginx/sites-enabled/
  ```

- [ ] Удален дефолтный конфиг:
  ```bash
  sudo rm /etc/nginx/sites-enabled/default
  ```

- [ ] Конфиг проверен:
  ```bash
  sudo nginx -t
  ```

- [ ] Nginx перезапущен:
  ```bash
  sudo systemctl restart nginx
  ```

### 7.3 Изменение порта Docker

- [ ] Изменен порт в `docker-compose.yml`:
  ```yaml
  ports:
    - "3000:3000"  # Вместо 80:3000
  ```

- [ ] Контейнер перезапущен:
  ```bash
  docker compose down
  docker compose up -d
  ```

### 7.4 Установка SSL сертификата

- [ ] Certbot установлен:
  ```bash
  sudo apt install certbot python3-certbot-nginx -y
  ```

- [ ] SSL сертификат получен:
  ```bash
  sudo certbot --nginx -d steins.ru -d www.steins.ru
  ```

- [ ] Email указан для уведомлений
- [ ] Согласие с ToS дано
- [ ] Выбрано перенаправление HTTP -> HTTPS

- [ ] Автообновление настроено:
  ```bash
  sudo systemctl status certbot.timer
  ```

- [ ] Тест обновления выполнен:
  ```bash
  sudo certbot renew --dry-run
  ```

### 7.5 Проверка HTTPS

- [ ] Сайт доступен по HTTPS:
  - Открыть: `https://steins.ru`

- [ ] Сертификат валидный (зеленый замок в браузере)

- [ ] HTTP редиректит на HTTPS

- [ ] Обновлен .env.production:
  ```env
  NEXT_PUBLIC_APP_URL=https://steins.ru
  ```

- [ ] Контейнер пересобран с новым URL:
  ```bash
  docker compose down
  docker compose build --no-cache
  docker compose up -d
  ```

---

## Этап 8: Финальные проверки

### 8.1 Производительность

- [ ] Использование ресурсов в норме:
  ```bash
  docker stats gate149-frontend
  ```

- [ ] Использование диска проверено:
  ```bash
  df -h
  docker system df
  ```

### 8.2 Логирование

- [ ] Логи контейнера доступны:
  ```bash
  docker compose logs -f
  ```

- [ ] Логи Nginx доступны (если используется):
  ```bash
  sudo tail -f /var/log/nginx/steins.ru.access.log
  ```

### 8.3 Автозапуск

- [ ] Контейнер настроен на автозапуск:
  - В `docker-compose.yml` есть `restart: unless-stopped`

- [ ] Проверка после перезагрузки (опционально):
  ```bash
  sudo reboot
  # Подождать 2 минуты, затем:
  ssh user@steins.ru
  docker ps | grep gate149
  ```

---

## Этап 9: Документация и backup

- [ ] Созданы алиасы для удобства (опционально):
  ```bash
  nano ~/.bashrc
  # Добавить алиасы из COMMANDS_CHEATSHEET.md
  source ~/.bashrc
  ```

- [ ] Создана директория для backups:
  ```bash
  mkdir -p ~/backups
  ```

- [ ] Создан скрипт backup (опционально):
  ```bash
  nano ~/backup-frontend.sh
  # Добавить команды backup
  chmod +x ~/backup-frontend.sh
  ```

- [ ] Документация сохранена для команды:
  - [ ] Записаны IP адреса
  - [ ] Записаны URL API и Kratos
  - [ ] Записаны команды для управления

---

## Этап 10: Мониторинг и поддержка

### Регулярные проверки

- [ ] Настроен мониторинг uptime (опционально)
- [ ] Настроены алерты (опционально)
- [ ] Планируются регулярные обновления

### Дальнейшие улучшения

- [ ] Настроить CI/CD для автоматического деплоя
- [ ] Настроить централизованное логирование (ELK)
- [ ] Установить мониторинг (Grafana + Prometheus)
- [ ] Настроить резервное копирование
- [ ] Оптимизировать Docker образ

---

## ✅ Деплой завершен успешно!

Если все пункты отмечены, поздравляем! Ваше приложение успешно задеплоено.

### Финальная проверка

✅ Сайт доступен: https://steins.ru  
✅ SSL работает  
✅ Страницы загружаются  
✅ API работает  
✅ Аутентификация работает  
✅ Логи без критических ошибок  

---

## 🆘 Если что-то пошло не так

1. **Проверьте логи:**
   ```bash
   docker compose logs -f
   ```

2. **Проверьте переменные окружения:**
   ```bash
   docker compose exec frontend env | grep -E "BACKEND|ORY|NEXT"
   ```

3. **Посмотрите раздел Troubleshooting в DEPLOYMENT.md**

4. **Используйте COMMANDS_CHEATSHEET.md для быстрого поиска команд**

5. **Проверьте ENV_VARS.md для правильной настройки переменных**

---

**Полезные ссылки:**
- [Полная документация](DEPLOYMENT.md)
- [Быстрый старт](QUICK_START.md)
- [Шпаргалка команд](COMMANDS_CHEATSHEET.md)
- [Переменные окружения](ENV_VARS.md)

**Версия:** 1.0  
**Дата:** 2025-11-20

